export interface User {
  user_id: string;
  minicode: string;
  enter_code: string;
  created_at: string;
}

export interface Judge {
  user_id: string;
  minicode: string;
  appointed_at: string;
}
